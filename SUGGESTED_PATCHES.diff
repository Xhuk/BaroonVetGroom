--- a/client/src/pages/Dashboard.tsx
+++ b/client/src/pages/Dashboard.tsx
@@ -131,10 +131,10 @@ export default function Dashboard() {
   }
 
   return (
-    <ResponsiveLayout>
+    <ResponsiveLayout>
       {/* Main Content */}
-      <div className="pb-40">
+      <div className="pb-40 container-fluid responsive-typography">
         {/* Action Buttons - Aligned with calendar card left edge */}
-        <div className="absolute top-[95px] left-[298px] flex gap-4 mb-6 z-10">
+        <div className="flex gap-4 mb-6 z-10 justify-start">
           <Link href="/booking">
             <Button className="bg-green-600 hover:bg-green-700 text-white px-6 py-3 shadow-md dark:bg-green-700 dark:hover:bg-green-800">
               <Phone className="w-4 h-4 mr-2" />

--- a/client/src/pages/Appointments.tsx
+++ b/client/src/pages/Appointments.tsx
@@ -101,7 +101,7 @@ const Appointments = memo(function Appointments() {
 
   // INSTANT UI - Always render immediately with skeleton
   return (
-    <div className="p-6 max-w-7xl mx-auto">
+    <div className="p-6 container-fluid responsive-typography">
       <div className="mb-4">
         <BackButton href="/" text="Volver al Dashboard" />
       </div>

--- a/client/src/pages/Clients.tsx
+++ b/client/src/pages/Clients.tsx
@@ -200,9 +200,9 @@ export default function Clients() {
 
   return (
     <div className="p-6 max-w-7xl mx-auto">
+    <div className="container-fluid responsive-typography">
       <div className="mb-4 flex items-center justify-between">
         <BackButton href="/" text="Volver al Dashboard" />
-        <div className="flex gap-2">
+        <div className="flex gap-2 flex-wrap">
           <Button
             onClick={() => setShowClientForm(!showClientForm)}
             className="bg-blue-600 hover:bg-blue-700 text-white"
@@ -269,7 +269,7 @@ export default function Clients() {
                   )}
                 </div>
               ) : (
-                <div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
+                <div className="auto-grid">
                   {filteredClients?.map((client) => (
                     <Card key={client.id} className="hover:shadow-md transition-shadow">
                       <CardHeader className="pb-3">

--- a/client/src/components/FastCalendar.tsx
+++ b/client/src/components/FastCalendar.tsx
@@ -161,7 +161,7 @@ export function FastCalendar({ appointments, className, selectedDate, onDateCha
             />
         ) : (
-          <div className="bg-card rounded-lg shadow-lg animate-pulse flex items-center justify-center h-96 tablet-card">
+          <div className="bg-card rounded-lg shadow-lg animate-pulse flex items-center justify-center tablet-card auto-grid" style={{ minHeight: '24rem' }}>
             <div className="text-muted-foreground">Cargando calendario...</div>
           </div>
         )}

--- a/client/src/components/ResponsiveNavigation.tsx
+++ b/client/src/components/ResponsiveNavigation.tsx
@@ -1,5 +1,6 @@
 import { useState, useEffect } from "react";
 import { Link, useLocation } from "wouter";
+import { useBreakpoint } from "@/useBreakpoint";
 import { cn } from "@/lib/utils";
 import { useAuth } from "@/hooks/useAuth";
 import { useTenant } from "@/contexts/TenantContext";
@@ -23,6 +24,7 @@ export function ResponsiveNavigation({ className }: { className?: string }) {
   const { shouldCollapseNavigation, deviceType, isTabletLandscape } = useScreenSize();
   const [location] = useLocation();
   const [isNavigating, setIsNavigating] = useState(false);
+  const isMobile = useBreakpoint('(max-width: 768px)');
 
   // Hide navigation in tablet landscape mode (ribbon takes over)
   if (isTabletLandscape) {
@@ -117,6 +119,10 @@ export function ResponsiveNavigation({ className }: { className?: string }) {
   console.log(`📱 ResponsiveNavigation: Device ${deviceType}, shouldCollapse: ${shouldCollapseNavigation}, landscape: ${isTabletLandscape}`);
 
   return (
+    <>
+    {isMobile && (
+      <div className="show-mobile">Mobile navigation placeholder</div>
+    )}
     <nav 
       className={cn(
         "fixed left-0 bg-card shadow-lg z-30 transform -translate-x-full lg:translate-x-0 transition-all duration-300 border-r border-border",
@@ -155,5 +161,6 @@ export function ResponsiveNavigation({ className }: { className?: string }) {
         ))}
       </div>
     </nav>
+    </>
   );
 }